const initialState = {
   posts: [],
   post: {},
   isFetching: false,
   error: ''
}

export const crudReducer = (state = initialState, action) => {
   switch (action.type) {
      default:
         return state
   }
}